var documenterSearchIndex = {"docs":
[{"category":"section","location":"approach/","page":"-","text":"in TraitMacros.jl how would you remove @traitdispatch as a user-facing macro and provide its functionality (a) either within @deftrati or when @trait_function is used [or when used for the first time for the current trait]. or (b) in some other way that is not user-facing.\n\nTraitMacros.jl already has the machinery for A2 fully built. Here's a clear breakdown of why A1 doesn't work and how A2 is wired:\n\n\n\nWhy A1 (@deftrait emits the dispatcher) doesn't work @deftrait only knows the trait name and its state-type mappings. It has no knowledge of which functions will use the trait — a single trait can serve any number of functions. You'd have to put function names inside @def_trait, which changes the public API significantly.\n\n\n\nHow A2 (first @trait_function auto-emits the dispatcher) works in this file\n\nThree pieces collaborate:\n\n**1. `_trait_for_state` registry** (line 12)  \n`@def_trait` populates this at expansion time:\n\n*   \n*   \n*   \n\nSo any later macro knows which trait owns a given state name.\n\n**2. `_dispatcher_emitted` set** (line 15)  \nTracks `(module, func_name)` pairs that already have a dispatcher, so the second `@trait_function foo(x)::StateB` for the same `foo` doesn't emit a duplicate.\n\n**3. `maybe_dispatcher_expr`** (line 124)  \nCalled by both `@trait_function` variants. On the first call for a given `(module, func_name)`:\n\n*   Looks up the trait name from the state\n*   Emits the full dispatcher (`function foo end`, the worker stub, the routing body)\n*   Marks the pair done\n\nOn all subsequent calls: returns `nothing`, only the worker method is added.\n\nThe **\"first wins\" consequence**: the dispatcher signature is frozen to whatever the first `@trait_function` call wrote. If you write:\n\n*   \n*   \n*   \n\nThe second call just adds a worker method — the public `foo` method still has the `x::Vector{Int}` constraint. This is usually correct (all implementations share the same public contract) but it's the principle tradeoff to be aware of.\n\n\n\nThe one remaining step to make @trait_dispatch truly non-user-facing Remove it from export. It stays internal as an escape hatch for edge cases (declaring a function before any implementations, or overriding the auto-inferred signature):\n\nThe macro itself is worth keeping in the module body — it's clean, it handles validation, and it's what maybedispatcherexpr mirrors. But there's no reason to advertise it.\n\n","title":"-"},{"category":"section","location":"approach/#Summary-of-the-design","page":"-","text":" @def_trait emits dispatcher @trait_function auto-emits (current) Just un-export\nAPI change Yes — functions listed in trait def None None\nWorks for multiple functions per trait No Yes Yes\nDispatcher visible to user No No No (unexported)\nEscape hatch available No Yes (@trait_dispatch) Yes\n\nOtion A2 as already implemented is the right choice. The only action needed is removing @trait_dispatch from export.\n\n","title":"Summary of the design"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"This page moves from the smallest useful pattern to more realistic extension scenarios.","title":"Examples"},{"category":"section","location":"examples/#Simple-Use","page":"Examples","text":"Use @def_trait when you want a single public function to route to different implementations based on the first argument's structural category.\n\n@def_trait Sequence begin\n    DynamicVector => [Vector]\n    StaticTuple   => [Tuple, NamedTuple]\nend\n\n@trait_dispatch describe_items(x) :: Sequence\n\n@trait_function describe_items(x) :: DynamicVector = \"vector length $(length(x))\"\n@trait_function describe_items(x) :: StaticTuple = \"tuple length $(length(x))\"\n\ndescribe_items([1, 2, 3])\n# \"vector length 3\"\n\ndescribe_items((1, 2, 3))\n# \"tuple length 3\"\n\nUse this shape when the public behavior is the same operation, but the implementation differs by category.","title":"Simple Use"},{"category":"section","location":"examples/#Common-Use:-Additional-Arguments","page":"Examples","text":"Only the first argument participates in trait resolution. The remaining arguments pass through unchanged.\n\n@def_trait SequenceScale begin\n    DynamicVectorScale => [Vector]\n    StaticTupleScale   => [Tuple]\nend\n\n@trait_dispatch scale_items(data, factor) :: SequenceScale\n\n@trait_function scale_items(data, factor) :: DynamicVectorScale = data .* factor\n@trait_function scale_items(data, factor) :: StaticTupleScale = map(x -> x * factor, data)\n\nscale_items([1, 2], 10)\n# [10, 20]\n\nscale_items((1, 2), 10)\n# (10, 20)\n\nThis is the common case when the trait selects the algorithm family and the remaining arguments provide configuration.","title":"Common Use: Additional Arguments"},{"category":"section","location":"examples/#Common-Use:-Preserve-A-Typed-Public-Boundary","page":"Examples","text":"The public signature is preserved exactly as written. That means ordinary Julia method filtering still happens before trait routing.\n\n@def_trait TypedSequence begin\n    TypedVectorState => [Vector]\nend\n\n@trait_dispatch process_int_vector(x::Vector{Int}) :: TypedSequence\n@trait_function process_int_vector(x::Vector{Int}) :: TypedVectorState = sum(x)\n\nprocess_int_vector([1, 2, 3])\n# 6\n\nprocess_int_vector(Float32[1, 2, 3])\n# MethodError on the public function\n\nUse this when the trait is only part of your contract and you still want ordinary Julia dispatch to enforce a narrower API.","title":"Common Use: Preserve A Typed Public Boundary"},{"category":"section","location":"examples/#Advanced-Application:-Open-World-Extension","page":"Examples","text":"Downstream code can extend an existing trait without editing the original @def_trait block.\n\n@def_trait SequenceExtension begin\n    DynamicVectorExtension => [Vector]\n    StaticTupleExtension   => [Tuple]\nend\n\n@trait_dispatch summarize_items(x) :: SequenceExtension\n\n@trait_function summarize_items(x) :: DynamicVectorExtension = (:dynamic, length(x))\n@trait_function summarize_items(x) :: StaticTupleExtension = (:static, length(x))\n\nstruct MyTree\n    leaves::Int\nend\n\nBase.length(tree::MyTree) = tree.leaves\n\n@trait_map SequenceExtension StaticTupleExtension => MyTree\n\nsummarize_items(MyTree(5))\n# (:static, 5)\n\nUse @trait_map for this path instead of writing low-level trait methods by hand. It keeps the extension aligned with the generated mappings.","title":"Advanced Application: Open-World Extension"},{"category":"section","location":"examples/#Advanced-Application:-Public-Docs-And-Private-Implementations","page":"Examples","text":"The public function is the stable API surface. The trait-specific methods are implementation detail entry points.\n\n@def_trait NormalizationStyle begin\n    MutableNormalization => [Vector]\nend\n\n\"\"\"\n    normalize_items(x)\n\nPublic entry point for normalization based on the `NormalizationStyle` trait.\n\"\"\"\n@trait_dispatch normalize_items(x) :: NormalizationStyle\n\n\"\"\"\nImplementation for mutable vectors.\n\"\"\"\n@trait_function normalize_items(x) :: MutableNormalization begin\n    total = sum(x)\n    return x ./ total\nend\n\nnormalize_items([2.0, 3.0, 5.0])\n# [0.2, 0.3, 0.5]\n\nDocumenting the macros this way keeps user-facing behavior attached to the public symbol rather than to the generated worker methods.","title":"Advanced Application: Public Docs And Private Implementations"},{"category":"section","location":"howmacroswork/","page":"-","text":"How the macros work (MacroTraits.jl — the active file) There are four macros. Together they implement the Holy Traits pattern where dispatch is routed through a type-level trait function rather than the argument type directly.\n\n@def_trait\n\n@def_trait SequenceTrait begin\n    DynamicVector => [Vector]\n    StaticTuple  => [Tuple, NamedTuple]\nend\n\nExpands to:\n\n\n1. abstract type SequenceTrait end   # the trait umbrella type\n\n2. A fallback SequenceTrait(x::Any) that throws MethodError for unmapped types\n\n3. For each mapping line:\n\nstruct DynamicVector <: SequenceTrait end  # a singleton state type\n\nSequenceTrait(::Vector) = DynamicVector()  # the trait classifier\n     (marked :foldable/:aggressive so the compiler eliminates it at compile time)\n\n@trait_dispatch\n\n@trait_dispatch process(x, y) :: SequenceTrait\n\nExpands to:\n\nfunction process end\n\nfunction __macrotraits_trait_worker__process end\n\nfunction process(x, y)\n    local _state = SequenceTrait(x)    # classify via trait\n    invoke_trait_worker(process, \n        __macrotraits_trait_worker__process,\n        _state, \n        x, y)\n\n                         # dispatch to worker\nend\n\nThe first argument is always the one routed through the trait. The worker name is mangled to avoid collisions. invoketraitworker re-raises MethodError against the public function if no worker method matches, hiding the internal indirection from the user.\n\n@trait_function\n\n@traitfunction process(x, y) :: DynamicVector = x .* y # or block form: @traitfunction process(x, y) :: StaticTuple begin      map(v -> v * y, x) end\n\nExpands to a method on the private worker:\n\nmacrotraitstraitworkerprocess(::DynamicVector, x, y) = x .* y The ::StateName annotation selects which trait branch this implements, not a type constraint on the arguments.\n\n@trait_map\n\n@traitmap SequenceTrait StaticTuple => MyType Adds a new classifier method after @deftrait, enabling open-world extension without reopening the original definition:\n\nSequenceTrait(::MyType) = StaticTuple() Call flow at runtime\n\nprocess([1,2], 3)\n  → SequenceTrait([1,2])          # returns DynamicVector()  → __macrotraits_trait_worker__process(DynamicVector(), [1,2], 3)  \n  → [3, 6]\n\nBecause SequenceTrait is annotated :foldable and :constprop :aggressive, Julia's compiler resolves the trait branch at compile time — the runtime call is a direct dispatch with zero overhead.\n\nWhat TraitMacros.jl contains That file holds a work-in-progress version with the auto-dispatch feature — it is not included from MacroTraits.jl yet, so it has no effect on the running package.","title":"-"},{"category":"section","location":"#MacroTraits.jl","page":"Home","text":"MacroTraits.jl implements a small trait-dispatch surface with four macros:\n\n@def_trait defines a trait and its built-in mappings.\n@trait_dispatch defines the public entry point.\n@trait_function defines trait-state-specific implementations.\n@trait_map extends an existing trait with new mappings.\n\nThe package is intentionally narrow. It routes on the trait of the first argument, preserves the public signature you wrote, and keeps the generated worker functions private.","title":"MacroTraits.jl"},{"category":"section","location":"#Quick-Start","page":"Home","text":"@def_trait Sequence begin\n    DynamicVector => [Vector]\n    StaticTuple   => [Tuple, NamedTuple]\nend\n\n@trait_dispatch process_items(x) :: Sequence\n\n@trait_function process_items(x) :: DynamicVector = :dynamic\n@trait_function process_items(x) :: StaticTuple = :static\n\nprocess_items([1, 2, 3])\n# :dynamic\n\nprocess_items((1, 2, 3))\n# :static","title":"Quick Start"},{"category":"section","location":"#What-To-Read-Next","page":"Home","text":"See Examples for progressive usage patterns from simple dispatch to open-world extension.\nSee Internals for how each macro expands conceptually and how the generated pieces interact.","title":"What To Read Next"},{"category":"section","location":"#Operational-Model","page":"Home","text":"At runtime, the public function computes TraitName(first_argument) and forwards the trait-state value plus the original arguments to a hidden worker generated by MacroTraits.\n\nIf trait resolution succeeds but no matching @trait_function implementation exists, the call fails as the public function rather than surfacing the hidden worker in the normal error path.","title":"Operational Model"},{"category":"section","location":"#Signature-Rules","page":"Home","text":"MacroTraits intentionally supports only simple positional signatures written as name or name::Type.\n\nSupported:\nf(x)\nf(x, y)\nf(x::Vector{Int})\nRejected during macro expansion:\ndefault values\nkeyword arguments\nvarargs\ndestructuring arguments\n\nThis narrow surface keeps the generated methods predictable and helps preserve clear error behavior.","title":"Signature Rules"},{"category":"section","location":"#Extension-Guidance","page":"Home","text":"Prefer @trait_map when extending an existing trait with new types. It emits the same foldable, constprop trait-resolution methods as @def_trait.\n\nFor manual trait-resolution methods, return a trait-state instance such as StaticTuple(), and treat MacroTraits worker functions as internal implementation details.","title":"Extension Guidance"},{"category":"section","location":"#Documentation-Placement","page":"Home","text":"Attach public API docstrings to @trait_dispatch. Attach implementation-specific docstrings to @trait_function methods.","title":"Documentation Placement"},{"category":"section","location":"internals/#Internals","page":"Internals","text":"This page describes how each macro contributes to the generated system and how the pieces work together.","title":"Internals"},{"category":"section","location":"internals/#Design-Intent","page":"Internals","text":"MacroTraits keeps the public surface small and the generated internals predictable.\n\nTrait resolution is ordinary Julia method dispatch.\nThe public function remains the user-facing boundary.\nWorker functions are private implementation detail.\nExpansion-time validation rejects signatures that would make the generated code ambiguous or surprising.","title":"Design Intent"},{"category":"section","location":"internals/#@def_trait","page":"Internals","text":"@def_trait TraitName begin ... end performs three jobs:\n\nIt defines the abstract trait type.\nIt defines one concrete trait-state type for each mapping line.\nIt emits trait-resolution methods like TraitName(::SomeType) = SomeState().\n\nConceptually, this:\n\n@def_trait Sequence begin\n    DynamicVector => [Vector]\n    StaticTuple   => [Tuple, NamedTuple]\nend\n\nproduces a trait type, concrete state markers, and foldable trait-resolution methods.\n\nIt also emits a fallback TraitName(x::Any) method that throws a MethodError. That keeps unmapped types failing at trait resolution rather than silently falling through to an unrelated implementation.","title":"@def_trait"},{"category":"section","location":"internals/#@trait_map","page":"Internals","text":"@trait_map TraitName StateName => TypeExpr extends an existing trait after the original declaration.\n\nIt reuses the same mapping-generation path as @def_trait, but only emits additional trait-resolution methods. It does not redefine the trait type or trait-state structs.\n\nThis is the intended open-world extension mechanism.","title":"@trait_map"},{"category":"section","location":"internals/#@trait_dispatch","page":"Internals","text":"@trait_dispatch f(args...) :: TraitName defines the public entry point and a namespaced hidden worker.\n\nThe expansion flow is:\n\nParse and validate the annotated call shape.\nPreserve the user-written public signature exactly.\nCompute TraitName(first_argument) inside the generated method.\nForward the resulting trait-state value plus the original arguments to the hidden worker.\n\nImportant properties:\n\nOnly the first argument determines the trait.\nThe public signature is not widened by MacroTraits.\nA gensym temporary avoids accidental local-name capture.\nA GlobalRef binds helper calls back to MacroTraits, so expansion in another module remains hygienic.","title":"@trait_dispatch"},{"category":"section","location":"internals/#@trait_function","page":"Internals","text":"@trait_function supplies the trait-state-specific implementations for the hidden worker.\n\nBoth supported forms:\n\n@trait_function f(x) :: SomeState begin\n    ...\nend\n\n@trait_function f(x) :: SomeState = value\n\nnormalize to the same internal worker-method shape. The first argument of the generated worker is the trait-state marker, followed by the original arguments.\n\nThis means the trait state is part of the internal dispatch key, while the public function remains clean for callers.","title":"@trait_function"},{"category":"section","location":"internals/#Macro-Interplay","page":"Internals","text":"The macros are intended to be used as a pipeline:\n\n@def_trait creates the trait vocabulary.\n@trait_dispatch creates the public function boundary.\n@trait_function fills in implementations for each trait state.\n@trait_map extends the vocabulary later when downstream code introduces new types.\n\nOperationally, a call looks like this:\n\nCaller invokes the public function.\nThe public function computes the trait from the first argument.\nThe generated code invokes the hidden worker with the trait-state instance and original arguments.\nJulia dispatch selects the matching worker method.","title":"Macro Interplay"},{"category":"section","location":"internals/#Error-Boundaries","page":"Internals","text":"MacroTraits deliberately keeps errors aligned with the public API.\n\nIf no trait mapping exists, trait resolution fails as TraitName(::Type).\nIf a trait mapping exists but no implementation exists, the failure is reshaped to the public function rather than exposing the internal worker in the normal error path.\nIf the signature shape is unsupported, macro expansion throws an ArgumentError before methods are generated.\n\nThis separation helps users understand whether the problem is missing mapping, missing implementation, or invalid macro input.","title":"Error Boundaries"},{"category":"section","location":"internals/#Supported-Signature-Surface","page":"Internals","text":"MacroTraits accepts only simple positional arguments in the forms name and name::Type.\n\nThe parser rejects:\n\ndefault values\nkeyword arguments\nvarargs\ndestructuring arguments\n\nThis is a deliberate constraint. It keeps the generated worker signatures straightforward and avoids hidden semantic differences between the public function and the worker methods.","title":"Supported Signature Surface"},{"category":"section","location":"internals/#Hygiene-And-Private-Boundaries","page":"Internals","text":"The implementation uses three important internal rules to keep macro expansion predictable:\n\nGenerated worker names are namespaced by MacroTraits rather than using a short _name convention.\nTemporary locals inside generated functions use gensym.\nInternal helper references are emitted with GlobalRef so caller modules do not need to define MacroTraits internals.\n\nThese details matter most when the macros are expanded in downstream modules or in codebases that already have similarly named bindings.","title":"Hygiene And Private Boundaries"}]
}
